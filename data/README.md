# TODO: put your collected source documents here

This folder is read by `notebooks/rag_pipeline.ipynb` (Phase 2.1, `DATA_DIR`).

- Drop your PDFs / .txt files directly in this folder (subfolders are fine, the
  notebook globs recursively).
- If your corpus is large, don't commit it — add it to `.gitignore` and instead
  document in the root README.md how a stranger can obtain/regenerate it.
